{
    "entry_point": "rl_qubo_optimizer:RLLocalSearchOptimizer",
    "default_params": {
      "time_limit": 5,
      "step_size": 5,
      "verbose": false
    },
    "optimizer_type": "Heuristic QUBO optimizer based on RL",
    "description": "Mix of a simulated annealing algorithm with a custom reward and update system.",
    "compatible_problems": [
      "QUBO"
    ],
    
    "parameters": {
      "time_limit": {
        "type": "int",
        "description": "Maximum optimization time in seconds."
      },
      "temperature": {
        "type": "int",
        "description": "Starting temperature for the annealing process."
      },
      "verbose": {
        "type": "bool",
        "description": "True to see optimization process in terminal output."
      }
    },
    "requirements": [
      "torch",
      "qubots",
      "numpy"
    ],
    "keywords": [
      "QUBO",
      "heuristic",
      "general QUBO solver"
    ]
}

